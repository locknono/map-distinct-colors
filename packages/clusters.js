import type point, { createPoint } from "./points";

type cluster = {
  classNumber: number,
  area: number
};

function createCluster(points: Array<point>): Array<cluster> {
  let clusters = [];

  return clusters;
}
